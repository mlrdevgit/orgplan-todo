################################################################################
# Task Backend Selection
################################################################################
# Choose which task management service to sync with
# Options: microsoft, google
TASK_BACKEND=microsoft

################################################################################
# Microsoft To Do Configuration (if TASK_BACKEND=microsoft)
################################################################################
# Authentication mode: "application" or "delegated"
# - application: Uses client credentials (no user interaction, requires admin consent)
# - delegated: Uses user login (OAuth flow, no admin consent needed)
AUTH_MODE=application

# Get these values from Azure AD app registration
# See docs/GRAPH_API_SETUP.md for setup instructions
MS_CLIENT_ID=your-microsoft-client-id-here
MS_TENANT_ID=your-microsoft-tenant-id-here

# For application mode (client credentials flow):
MS_CLIENT_SECRET=your-microsoft-client-secret-here

# For delegated mode (authorization code flow):
# No additional config needed - tokens are stored automatically in .tokens/
# On first run, you'll be prompted to authenticate via browser

# Microsoft To Do list name
TODO_LIST_NAME=Orgplan 2025

################################################################################
# Google Tasks Configuration (if TASK_BACKEND=google)
################################################################################
# Get these values from Google Cloud Console
# See docs/GOOGLE_TASKS_SETUP.md for setup instructions
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Google task list name (leave empty to use primary list)
GOOGLE_TASK_LIST_NAME=

################################################################################
# Common Configuration
################################################################################
# Orgplan directory (default: current directory)
ORGPLAN_DIR=.

# Optional: Override current month (format: YYYY-MM)
# SYNC_MONTH=2025-12

# Optional: Token storage path (for delegated/OAuth modes)
# TOKEN_STORAGE_PATH=.tokens/

# Optional: Log file path
# LOG_FILE=sync.log
